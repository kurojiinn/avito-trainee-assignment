# Сервис назначения ревьюеров для Pull Request'ов

Микросервис для автоматического назначения ревьюеров на Pull Request'ы (PR), а также управления командами и участниками.

## Описание

Сервис предоставляет HTTP API для:
- Управления пользователями (создание, обновление, удаление, получение)
- Управления командами (создание, обновление, удаление, получение)
- Создания PR с автоматическим назначением ревьюеров (до 2 активных ревьюверов из команды автора)
- Переназначения ревьюверов (из команды заменяемого ревьювера)
- Объединения PR (идемпотентная операция)
- Получения списка PR, назначенных конкретному пользователю

## Технологический стек

- **Язык**: Go 1.21
- **База данных**: PostgreSQL 15
- **HTTP роутер**: Gorilla Mux
- **Миграции**: Goose
- **Контейнеризация**: Docker & Docker Compose

## Быстрый старт

### Требования

- Docker и Docker Compose
- Go 1.21+ (для локальной разработки)

### Запуск с Docker Compose

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd avito-trainee-assignment
```

2. Запустите сервис:
```bash
docker-compose up
```

Сервис будет доступен на `http://localhost:8080`

Миграции применяются автоматически при запуске через отдельный сервис `migrate`.

### Локальная разработка

1. Установите зависимости:
```bash
make install-deps
```

2. Запустите PostgreSQL (или используйте docker-compose только для БД):
```bash
docker-compose up -d db
```

3. Примените миграции:
```bash
make migrate-up
```

4. Запустите сервис:
```bash
make run
```

## API Endpoints

### Health Check
- `GET /health` - Проверка работоспособности сервиса

### Users
- `POST /api/v1/users` - Создать пользователя
- `GET /api/v1/users/{user_id}` - Получить пользователя
- `PUT /api/v1/users/{user_id}` - Обновить пользователя
- `DELETE /api/v1/users/{user_id}` - Удалить пользователя
- `GET /api/v1/users/{user_id}/pull-requests` - Получить список PR, назначенных пользователю

### Teams
- `POST /api/v1/teams` - Создать команду
- `GET /api/v1/teams/{team_id}` - Получить команду
- `PUT /api/v1/teams/{team_id}` - Обновить команду
- `DELETE /api/v1/teams/{team_id}` - Удалить команду

### Pull Requests
- `POST /api/v1/pull-requests` - Создать PR (автоматически назначаются ревьюверы)
- `GET /api/v1/pull-requests` - Получить все PR
- `GET /api/v1/pull-requests/{pull_request_id}` - Получить PR по ID
- `POST /api/v1/pull-requests/{pull_request_id}/reassign` - Переназначить ревьювера
- `POST /api/v1/pull-requests/{pull_request_id}/merge` - Объединить PR

## Примеры использования

### Создание команды
```bash
curl -X POST http://localhost:8080/api/v1/teams \
  -H "Content-Type: application/json" \
  -d '{"team_name": "Backend Team"}'
```

### Создание пользователя
```bash
curl -X POST http://localhost:8080/api/v1/users \
  -H "Content-Type: application/json" \
  -d '{
    "username": "john_doe",
    "team_id": "<team_id>",
    "is_active": true
  }'
```

### Создание PR
```bash
curl -X POST http://localhost:8080/api/v1/pull-requests \
  -H "Content-Type: application/json" \
  -d '{
    "pull_request_name": "Add new feature",
    "author_id": "<user_id>"
  }'
```

При создании PR автоматически назначаются до 2 активных ревьюверов из команды автора (исключая самого автора).

### Переназначение ревьювера
```bash
curl -X POST http://localhost:8080/api/v1/pull-requests/<pr_id>/reassign \
  -H "Content-Type: application/json" \
  -d '{
    "reviewer_id": "<reviewer_id_to_replace>"
  }'
```

Новый ревьювер выбирается случайным образом из команды заменяемого ревьювера.

### Объединение PR
```bash
curl -X POST http://localhost:8080/api/v1/pull-requests/<pr_id>/merge
```

Операция идемпотентна - повторный вызов не приводит к ошибке.

## Makefile команды

- `make build` - Собрать приложение
- `make run` - Запустить приложение локально
- `make test` - Запустить тесты
- `make test-coverage` - Запустить тесты с покрытием кода
- `make docker-build` - Собрать Docker образ
- `make docker-up` - Запустить сервисы через Docker Compose
- `make docker-down` - Остановить сервисы
- `make docker-logs` - Показать логи API сервиса
- `make migrate-up` - Применить миграции
- `make migrate-down` - Откатить последнюю миграцию
- `make migrate-status` - Показать статус миграций
- `make migrate-create name=<name>` - Создать новую миграцию
- `make install-deps` - Установить зависимости
- `make fmt` - Форматировать код
- `make vet` - Проверить код с помощью go vet
- `make clean` - Удалить артефакты сборки

## Конфигурация

Сервис использует переменные окружения для конфигурации:

- `POSTGRES_HOST` - Хост PostgreSQL (по умолчанию: localhost)
- `POSTGRES_PORT` - Порт PostgreSQL (по умолчанию: 5432)
- `POSTGRES_USER` - Пользователь PostgreSQL (по умолчанию: avito)
- `POSTGRES_PASSWORD` - Пароль PostgreSQL (по умолчанию: avito)
- `POSTGRES_DB` - Имя базы данных (по умолчанию: mydatabase)
- `PORT` - Порт HTTP сервера (по умолчанию: 8080)

## Архитектура

Проект следует принципам чистой архитектуры:

```
cmd/api/              # Точка входа приложения
internal/
  api/handlers/       # HTTP обработчики
  config/             # Конфигурация
  db/                 # Подключение к БД
  model/              # Модели данных
  repository/         # Слой доступа к данным
  service/            # Бизнес-логика
migrations/           # SQL миграции
```

## Особенности реализации

### Назначение ревьюверов
- При создании PR автоматически назначаются до 2 активных ревьюверов из команды автора
- Автор исключается из списка кандидатов
- Если доступных кандидатов меньше двух, назначается доступное количество (0/1)
- Пользователи с `isActive = false` не назначаются

### Переназначение ревьюверов
- Новый ревьювер выбирается из команды заменяемого ревьювера
- Исключаются автор PR и текущий ревьювер
- После объединения PR (статус MERGED) переназначение запрещено

### Идемпотентность merge
- Операция merge идемпотентна - повторный вызов не приводит к ошибке
- Возвращается актуальное состояние PR

## Вопросы и решения

### Отсутствие OpenAPI спецификации
В задании упоминается, что OpenAPI-спецификация будет предоставлена отдельным файлом `openapi.yaml`, но файл не был предоставлен. Реализован API на основе описания в задании и стандартных практик REST API.

### Выбор библиотеки для миграций
Использована библиотека `goose` для миграций, так как она проста в использовании и хорошо интегрируется с PostgreSQL.

### Структура базы данных
- Использованы UUID для всех идентификаторов для обеспечения уникальности
- Созданы индексы для оптимизации запросов
- Использована связь many-to-many для связи PR и ревьюверов через таблицу `pr_reviewers`

### Обработка ошибок
- Используются стандартные HTTP коды ответов
- Ошибки возвращаются в виде текстовых сообщений
- Проверка существования сущностей перед операциями

## Производительность

Сервис оптимизирован для умеренных объёмов данных:
- До 20 команд
- До 200 пользователей
- RPS: 5
- SLI времени ответа: 300 мс
- SLI успешности: 99.9%

Для обеспечения производительности:
- Созданы индексы на часто используемых полях
- Используются транзакции для атомарности операций
- Минимизировано количество запросов к БД

## Тестирование

Проект включает комплексные тесты для всех слоев приложения:

### Запуск тестов

```bash
# Запустить все тесты
make test

# Запустить тесты с покрытием кода
make test-coverage
```

### Структура тестов

- **Repository tests** (`internal/repository/*_test.go`) - Интеграционные тесты для слоя доступа к данным
- **Service tests** (`internal/service/*_test.go`) - Тесты бизнес-логики
- **Handler tests** (`internal/api/handlers/*_test.go`) - HTTP интеграционные тесты

### Требования для тестов

Тесты используют отдельную тестовую базу данных. Убедитесь, что PostgreSQL доступен и переменные окружения настроены:

```bash
export POSTGRES_HOST=localhost
export POSTGRES_PORT=5432
export POSTGRES_USER=avito
export POSTGRES_PASSWORD=avito
export POSTGRES_DB=test_database
```

Тесты автоматически создают и удаляют тестовую базу данных.

## Дополнительные задания

### ✅ Эндпоинт статистики

Реализован эндпоинт `/api/v1/statistics`, который возвращает:
- Общее количество назначений ревьюверов
- Статистику назначений по пользователям (топ-20)
- Статистику назначений по PR (топ-20)
- Общую статистику по PR (всего, открытых, мерженых)
- Среднее количество ревьюверов на PR

**Пример запроса:**
```bash
curl http://localhost:8080/api/v1/statistics
```

### ✅ Массовая деактивация пользователей команды

Реализован эндпоинт `POST /api/v1/teams/{team_id}/deactivate-members`, который:
- Массово деактивирует всех активных пользователей команды
- Безопасно переназначает ревьюверов для открытых PR
- Возвращает количество деактивированных пользователей

**Пример запроса:**
```bash
curl -X POST http://localhost:8080/api/v1/teams/{team_id}/deactivate-members
```

**Оптимизация:** Операция оптимизирована для укладывания в 100 мс для средних объёмов данных:
- Используются эффективные SQL-запросы с JOIN
- Минимизировано количество обращений к БД
- Используются транзакции для атомарности

### ✅ E2E тестирование

Реализованы комплексные E2E тесты в `tests/e2e_test.go`, которые проверяют:
- Полный workflow создания команды, пользователей, PR
- Автоматическое назначение ревьюверов
- Переназначение ревьюверов
- Объединение PR с проверкой идемпотентности
- Массовую деактивацию пользователей

**Запуск E2E тестов:**
```bash
go test -v ./tests/...
```

### ✅ Конфигурация линтера

Настроен `golangci-lint` с конфигурацией в `.golangci.yml`:
- Включены основные линтеры: errcheck, govet, golint, gosec, staticcheck
- Настроены правила для тестовых файлов
- Оптимизированы настройки для проекта

**Установка и запуск:**
```bash
# Установка golangci-lint
go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest

# Запуск линтера
golangci-lint run
```

### ✅ Нагрузочное тестирование

Создан скрипт `load_test.sh` для нагрузочного тестирования и документация в `LOAD_TESTING.md`.

**Запуск нагрузочного тестирования:**
```bash
./load_test.sh
```

Скрипт тестирует:
- Health check endpoint
- Statistics endpoint
- Create PR endpoint

**Результаты:** Система соответствует требованиям SLI:
- Время ответа < 300 мс
- Успешность 99.9%
- RPS = 5

## Документация

### 📚 Документация для разработчиков

1. **[CODE_DOCUMENTATION.md](CODE_DOCUMENTATION.md)** - Полная документация по архитектуре, структуре проекта и бизнес-логике
2. **[QUICK_START.md](QUICK_START.md)** - Быстрый старт для разработчиков с примерами и объяснениями
3. **[LOAD_TESTING.md](LOAD_TESTING.md)** - Документация по нагрузочному тестированию

### 💬 Комментарии в коде

Весь код содержит подробные комментарии на русском языке, объясняющие:
- Назначение функций и методов
- Бизнес-правила и логику работы
- Параметры и возвращаемые значения
- Алгоритмы и оптимизации
- Поток выполнения операций

### 🔍 Структура проекта

```
avito-trainee-assignment/
├── cmd/api/              # Точка входа приложения
├── internal/
│   ├── api/handlers/     # HTTP обработчики
│   ├── service/          # Бизнес-логика
│   ├── repository/       # Доступ к данным
│   ├── model/            # Модели данных
│   ├── config/           # Конфигурация
│   └── db/               # Подключение к БД
├── migrations/           # SQL миграции
└── tests/                # E2E тесты
```

**Рекомендуемый порядок изучения:**
1. Начните с `QUICK_START.md` для общего понимания
2. Изучите `cmd/api/main.go` для понимания структуры
3. Прочитайте `CODE_DOCUMENTATION.md` для деталей
4. Изучите код с комментариями для глубокого понимания

## 📚 Документация

> **🚀 Начните с [SUMMARY.md](SUMMARY.md) для быстрого обзора проекта!**

Проект включает подробную документацию:

- **[CODE_DOCUMENTATION.md](CODE_DOCUMENTATION.md)** - полная техническая документация проекта
  - Архитектура и структура проекта
  - Описание всех слоев приложения
  - API endpoints с примерами
  - Схема базы данных
  - Бизнес-логика и правила
  - Обработка ошибок
  - Тестирование и развертывание

- **[QUICK_START.md](QUICK_START.md)** - быстрый старт для разработчиков
  - Архитектура в двух словах
  - Как работает создание PR (полный путь запроса)
  - Основные концепции
  - Как добавить новый функционал
  - Полезные команды
  - Частые вопросы

- **[COMMENTS_GUIDE.md](COMMENTS_GUIDE.md)** - руководство по комментариям в коде
  - Типы комментариев
  - Стиль комментариев
  - Примеры хороших комментариев
  - Что не комментировать

- **[LOAD_TESTING.md](LOAD_TESTING.md)** - результаты нагрузочного тестирования
  - Методология тестирования
  - Сценарии тестирования
  - Результаты и оптимизации

## Лицензия

Это тестовое задание для стажёра Backend (осенняя волна 2025).

